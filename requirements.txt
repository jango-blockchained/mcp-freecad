# Core dependencies (sync with pyproject.toml)
modelcontextprotocol>=0.1.0
trio>=0.22.0
loguru>=0.7.0
requests>=2.28.0
psutil>=5.9.0

# API support - Python 3.13 compatible versions
fastapi>=0.100.0
pydantic>=2.0.0

# Additional dependencies found in code
flask>=3.0.0
fastmcp>=0.4.0
aiohttp>=3.8.0
mcp>=1.0.0

# FreeCAD-specific dependencies
PySide2
