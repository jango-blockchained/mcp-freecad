# Core MCP Server Dependencies
modelcontextprotocol>=0.1.0
trio>=0.22.0

# For AppImage download script
requests>=2.28.0

# Optional: Add if using FastAPI/Uvicorn for a web transport (not needed for stdio)
# fastapi>=0.95.0
# uvicorn[standard]>=0.22.0

# Testing (Optional)
# pytest>=7.0.0
# pytest-asyncio>=0.20.0
